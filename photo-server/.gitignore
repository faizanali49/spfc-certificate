# Dependencies
node_modules/

# Environment variables — NEVER commit these
.env

# Uploaded photos — these are real applicant data, not source code.
# Keep the folder structure in git, but not the actual uploaded files.
uploads/*
!uploads/.gitkeep

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db
